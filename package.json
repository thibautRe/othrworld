{
  "name": "@othrworld/monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "scripts": {
    "start": "yarn workspace @othrworld/app tscbuild && yarn workspace @othrworld/app start",
    "start-legacy": "node scripts/start.js"
  }
}
